<?php

/**
 * The display concept is migrated from Drupal where a display is used for one or more content types.
 *
 * This concept is the most useful when you have a lot of content types that share the same display and they might have
 * some small differences.
 *
 * For example the display of "card" for news and events is almost the same with the only is events having a date and
 * news having a tag.
 */
