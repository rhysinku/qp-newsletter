import addAttributes from "./attributes";
import ToolbarControl from "./toolbar-control";
import get from "./get";
import className from "./classname";

export const ContentPosition = props => null;

ContentPosition.addAttributes = addAttributes;
ContentPosition.ToolbarControl = ToolbarControl;
ContentPosition.get = get;
ContentPosition.className = className;
