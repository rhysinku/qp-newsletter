const baseUri = `${window.location.protocol}//${window.location.host}/wp-content/themes/qp-newsletter`;

export const blocksUri = `${baseUri}/gutenberg/blocks`;
export const spriteUri = `${baseUri}/assets`;
